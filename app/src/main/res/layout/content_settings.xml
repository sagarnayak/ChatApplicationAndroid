<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>

        <variable
            name="context"
            type="com.sagar.android.chatapp.ui.settings.Settings" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior"
        tools:context=".ui.settings.Settings"
        tools:showIn="@layout/activity_settings">

        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/textViewMessage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="@font/open_sans"
            android:padding="16dp"
            android:text="You can logout of all your logged in device to keep your account secured."
            android:textColor="@color/primary_text"
            android:textSize="@dimen/paragraphText"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/buttonLogoutAll"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:layout_marginEnd="16dp"
            android:backgroundTint="@color/colorPrimary"
            android:fontFamily="@font/open_sans_semibold"
            android:onClick="@{context::onClickLogoutAll}"
            android:text="Logout from All Devices"
            android:textColor="@android:color/white"
            android:textSize="@dimen/buttonAndTabs"
            app:layout_constraintEnd_toEndOf="@+id/textViewMessage"
            app:layout_constraintTop_toBottomOf="@+id/textViewMessage" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>